/**
 * Static application configuration.
 * These values will be wired to a settings UI in a future update.
 */
export const appConfig = {
  /**
   * When false, commands and plans are executed immediately without asking for
   * user confirmation. Set to true to restore the approval flow.
   * Note: destructive commands will always prompt regardless of this setting.
   */
  requireConfirmation: false,
};
